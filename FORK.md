# Fork 维护说明

本仓库是 [a9gent/mindfs](https://github.com/a9gent/mindfs) 的长期维护 fork：

- `origin`   → https://github.com/nightking8342/mindfs （本 fork，主干 `main`）
- `upstream` → https://github.com/a9gent/mindfs （上游）

维护原则：**控制 diff 面积**。能加新文件就不改上游文件，能走配置（`agents.json`、环境变量）就不动核心代码，让每次上游合并的冲突尽量少。

## 与上游的分歧清单

每一处与上游的故意分歧都记录在这里。合并上游遇到冲突时，按「归属」列决定保留哪边；新增分歧时必须同步更新本清单。

| 文件 / 范围 | 分歧内容 | 冲突归属 |
|---|---|---|
| `FORK.md`、`.github/workflows/ci.yml` | fork 独有文件，上游没有 | 我方 |
| `CLAUDE.md` 的「Fork 说明」小节 | fork 独有小节；若上游日后自己加了 CLAUDE.md，合并其内容但保留本小节 | 手动合并 |
| `web/package.json` 的 `allowScripts` 字段 | 允许 esbuild 安装脚本（本地构建需要） | 我方（版本号等其余字段随上游） |
| Android WebView 文字缩放设置与 IME 选区修复（e587493） | fork 自研功能，上游未合入 | 我方 |
| `web/src/App.tsx` 的 `TOKEN_STATION_ENABLED` 开关 | 置 `false` 隐藏官方 Token 加油站入口并停掉其后台轮询；恢复或指向自建中转站时置 `true` | 我方（开关及三处引用；面板 JSX 本体随上游） |
| fork UI 隔离层（`ForkShell` + `fork-theme.css`） | fork 要改 UI 布局与视觉，但上游 `App.tsx` 有 1.5 万行、布局装配在文件尾部，直接改必然每次合并都在巨型文件里解冲突。改为接管布局壳：新增 `web/src/layout/ForkShell.tsx`（实现与上游 `AppShell` 相同的五槽位接口，尺寸走 `--fork-*` CSS 变量，各区域挂 `data-fork-region` / `data-fork-viewport` 钩子）+ `web/src/styles/fork-theme.css`（视觉覆盖层，加载于 `index.css` 之后）。**上游 `layout/AppShell.tsx` 与 `index.css` 一律不改**，对上游文件的 diff 仅两行 import。`ForkShell` 内含 props 契约断言（`Equals<ForkShellProps, UpstreamShellProps>`），上游增删改 `AppShell` 的 prop 时 `npm run typecheck` 直接失败，据此同步；`?forkShell=0` 可临时回退上游 `AppShell` 用于合并后对照排查 | 我方（`web/src/layout/ForkShell.tsx`、`web/src/styles/fork-theme.css` 为 fork 独有文件；`App.tsx` 的 `import { ForkShell as AppShell }` 一行与 `main.tsx` 的 `import "./styles/fork-theme.css"` 一行。上游改 `AppShell.tsx` 内部实现时**取上游版本**（该文件仍归上游），但 `ForkShell` 不会自动继承这些改动，需按同步流程第 7 步逐条评估是否 port——尤其是移动端踩坑补丁；props 形状变化则由契约断言在 `typecheck` 阶段强制暴露） |
| 配置备份编辑 + Claude 单独 settings（`docs/agent-config-backup-edit-spec.md`） | 备份清单可更新；快照文件读写；Claude `isolatedClaudeSettings` 切换写独立路径并用 `WithSettingsPath`，不覆盖用户 `~/.claude/settings.json` | 我方（`server/internal/api/agent_config.go` + `http.go` 路由、`preferences/store.go`、`agent/claude/session.go` + `pool.go` + `types/types.go`、`api/usecase/session.go`、`web/src/services/agentConfig.ts`、`web/src/components/FileTree.tsx` 的 `AgentConfigPopover`、两份 i18n locale） |
| 配置切换进度展示（`docs/agent-config-switch-progress-spec.md`） | `switchAgentConfig` 返回步骤清单（失败时也带）；探活结束无条件广播 `agent.config.switched`，因为 `agent.status.changed` 会被 `Prober.statusChanged` 过滤掉 | 我方（`agent_config.go`、`appcontext.go`、`helpers.go` 的 `respondErrorWithExtra`、`agent_api_provider.go` 调用处、`App.tsx`、`FileTree.tsx`、`services/agentConfig.ts`、`services/error.ts` 的 `agent.config_switched`） |
| Agent 配置弹层点击外部关闭 | 上游只在「选择 Agent」一步监听外部点击；fork 扩展到全部步骤（全屏文件编辑器 `file` 步骤除外，避免丢草稿） | 我方（`web/src/components/FileTree.tsx` 的 click-outside effect） |
| 备份清单持久化的抗截断处理 | 上游 `manifest.json` / `agents-env.json` 用 `os.WriteFile` 非原子写（先截断再写），且读到 0 字节文件时静默当作「无备份」；两者叠加使一次中断的写入被后续「创建备份」固化——清单被覆盖成仅剩新建的一条，而快照目录仍在磁盘上。fork 改为 `writeFileAtomic` 写入，并把 0 字节文件视为损坏直接报错（正常写入最小产物是 `[]` / `{}`，不会为空） | 我方（`server/internal/api/agent_config.go` 的 `read/writeAgentConfigManifest`、`read/writeAgentEnvBackups`；新增 `agent_config_manifest_test.go`。上游日后自行修同类问题可评估回退） |
| `agent_config_test.go` 的 Windows 配置目录隔离 | 上游只设 `HOME` / `XDG_CONFIG_HOME`，而 Windows 的 `os.UserConfigDir()` 读 `%AppData%`，导致该测试在 Windows 上直接读写**真实用户**的备份清单并将其覆盖；fork 补设 `AppData` / `USERPROFILE`（同文件内 `setupAgentConfigTest` 早已这么做） | 我方（上游日后补齐隔离则随上游并删除本行） |
| `switchAgentConfig` 的 `apply_env` 步骤状态 | fork 规格 `agent-config-switch-progress-spec.md` §4.3 要求「备份无 env 时跳过 `apply_env`」，但实现一律记 `ok`，与 fork 自带测试 `TestSwitchSkipsEnvStepWhenBackupHasNoEnv` 矛盾；改为无 `envKeys` 时记 `skipped`，同时保留必须执行的旧 env 清除动作 | 我方（属「配置切换进度展示」功能范围） |
| `web/tests/agent-lifecycle-restart.test.mjs` 的英文文案断言 | 上游该断言与其自身 `en-US.ts` 实际文案不一致（v0.4.4 tag 内自相矛盾，upstream/main 亦未修）；fork 把断言对齐实际文案 `Agent config switch & restart` | 我方（上游日后若统一文案与断言，随上游并删除本行） |
| Codex 主题（`data-theme="codex"`） | fork 自建的深色主题：中性纯黑 `#0a0a0a` 配 `#10a37f`，去掉渐变与投影（上游 `dark` 是 slate 蓝黑 `#0F172A` 配蓝色强调）。主题 CSS 本体全部写在 fork 独有的 `fork-theme.css`，**不改 `index.css`**；变量覆盖度对齐上游 `dark`（42 个变量一个不漏，避免 fallback 成浅色）。`getEffectiveAppearanceMode` 把 `codex` 映射为 `dark` 而非返回自身，因为 `ActionBar` 用它的返回值选前景色，判成浅色会让 `#1d4ed8` 之类的深蓝文字落在纯黑背景上 | 我方（`web/src/styles/fork-theme.css` 的 codex 块为 fork 独有；上游文件共 6 行「列表加一项」型追加：`services/appearance.ts` 的类型联合 / 白名单 Set / `themeColors` / `getEffectiveAppearanceMode` 的映射分支、`components/FileTree.tsx` 的 `APPEARANCE_OPTIONS`、两份 i18n locale 的 `appearance.codex`。上游若自行新增主题，冲突形态是两边各加一行，直接都保留） |
| Liquid Glass 主题（`data-theme="glass"`） | fork 自建的**深色中性**玻璃主题。核心认知：**玻璃本身几乎无色**，颜色来自背后壁纸透过来。往 `--panel-bg` 等变量里塞彩色渐变等于给玻璃上色，会越加越浑、压住文字——这是走过的弯路，勿再犯。正确做法是四层：① 玻璃层＝**均匀**的半透明白 + 噪点（`feTurbulence`，纯渐变永远像塑料）；② `backdrop-filter` 吸取背后颜色；③ `inset` 顶亮边/发丝边/底反光＝厚度，外阴影＝浮起（缺外阴影时圆角区块看着像挖出来的洞）；④ 壁纸是全主题唯一颜色来源，强度必须压到「若隐若现」（alpha ≤0.06）且用冷色，`saturate` 也不能高——参考图里卡片其实是中性深灰，绿只在按钮图标上。<br>**「一块完整的玻璃」靠板块化实现**：三大区域各加 `margin` + `border-radius`（`--fork-glass-gap` / `--fork-glass-radius`），边框走 `--fork-*-border: none`，让壁纸从缝隙透出。紧贴的三栏只有一条细线分隔，无论怎么调材质都不像「一块板」。移动端排除（侧栏是全屏抽屉）。<br>另两个易踩点：ForkShell 三大区域的 fallback 链是 `var(--fork-sidebar-bg, var(--mindfs-topbar-bg, var(--sidebar-bg)))`，`--mindfs-topbar-bg` 排在前面，只设 `--sidebar-bg` 会被吃掉；壁纸光斑要按三栏布局分布，放四角会被两侧栏挡住、主区剩一片死黑。<br>`ForkShell` 在此主题下把 footer 移进主区浮起来（移动端除外，那里 footer 要参与 flex 给软键盘让位），并给主区 `padding-bottom` 避免内容被永久遮挡。`getEffectiveAppearanceMode` 读 `data-scheme` 决定明暗（见下一条）。<br>**浅色变体**：`[data-scheme="light"]` 提供。浅色玻璃比深色难做——白玻璃贴白背景对比趋近于零，所以策略相反：壁纸色彩要浓得多（alpha≈0.30 对深色版的 0.055），玻璃更白更实，高光也从「白色 inset 提亮」改为「彩色外阴影压层次」。强调色在浅色下必须调深到 `#00814a`（对比度 4.79），原来的 `#00a862` 只有 2.99、不达 AA。带 `prefers-reduced-transparency` 降级。<br>**壁纸顶部必须先铺一段纯 `--bg-gradient-start` 再淡入光斑**（`--fork-wallpaper-top-fade`，132px），否则 Android 沉浸状态栏下会有一道横向接缝：状态栏那块 scrim 取的是 `--mindfs-system-bar-bg`（`#e8eefb` 实色），而原生把 WebView 整体下推了状态栏高度（`MainActivity` 的 `view.setPadding(0, systemInsets.top, 0, 0)`，注入给页面的 `--mindfs-safe-area-top` 反而是 0），**页面 `y=0` 就贴着状态栏底边**——光斑铺到 `y=0` 时那里是 `#fad3e8`，与状态栏差 27/255，肉眼很明显。配套要把上面两团光斑的中心下移（`6%`→`20%`、`16%`→`28%`），否则大半被遮罩吃掉、显得被削平。淡出色写同色 alpha 0 而非 `transparent`。深色版不需要这层：光斑 alpha 只有 0.055，底色与状态栏同为纯黑。<br>**内容块玻璃化的两条硬约束**（首页看板 + Markdown 代码块/表格共用）：① 外阴影一律负 spread 收进自身轮廓——这些块外面套着 `overflow: hidden/auto` 的容器（看板的横向滚动条、对话气泡），扩散出去的部分会被硬切成直角，三块看板的阴影还会先在缝隙里糊成一片、再被裁出一个「不透明底板」的假象；② 边缘光挂 `::before` 还是挂元素自身取决于谁在滚动——`<pre>`、表格外壳自己就是 `overflow: auto`，绝对定位的伪元素会跟着内容滚走，只能用元素自身的 inset 阴影。<br>**Markdown 代码块与表格**走 `.markdown-viewer` 下的 `pre[class]` / `div:has(> table)` / `thead` / `td`，对话流与文件预览共用同一个 `MarkdownViewer` 因而一处生效两处。代价是隐式依赖上游该组件的渲染结构：上游给表格外壳加 class、改 inline style 或换掉包裹 div，这些规则会**静默失效**（不报错，只是玻璃没了），同步上游时值得扫一眼 `MarkdownViewer.tsx`。`pre[class]` 的限定是为了放过 Mermaid 的报错块（它没有 class 属性），别把红框红字洗成玻璃 | 我方（`fork-theme.css` 的 glass 块 + `ForkShell.tsx` 的 `useThemeId` / `floatingFooter`，均为 fork 独有文件；上游文件与 codex 主题共用同样的 6 个改动点，各再加一行） |
| 明暗维度与主题正交（`data-scheme`） | 上游把「跟随系统」当成主题列表里的一项（`system`），于是「meadow 且跟随系统」这类组合无法表达，而 `dark`/`light` 其实只是同一套默认配色的两个变体——两个正交维度被压成了一维。fork 拆开：`services/forkScheme.ts` 用 `THEME_SCHEMES` 声明每个主题支持的形态，把「用户偏好 × 系统偏好 × 主题支持」解析成**最终值**写进 `<html data-scheme>`，CSS 只认这个属性。<br>关键收益：`fork-theme.css` 的媒体查询归零，每套配色值只写一遍——否则「浅色」「浅色 + 减少透明度」这类条件会变成笛卡尔积。深色是 `[data-theme="glass"]` 的默认值，`[data-scheme="light"]` 才覆盖，因此 JS 未执行时退化成深色而非变量全丢白屏。<br>单模式主题（codex 仅深色，meadow/moss 仅浅色）忽略用户偏好，UI 上开关置灰并标注原因——能点但无反应会被当成 bug。上游 `system` **刻意不在** `THEME_SCHEMES` 表内，保持「移除 `data-theme` 交给 `index.css` 媒体查询」的原有行为。<br>`getEffectiveAppearanceMode` 改为读 `data-scheme` 而非硬编码主题名（原先是 `if (mode === "codex") return "dark"`），与 CSS 同源，将来新增 fork 主题不必再改它。`forkScheme.ts` import 即自执行，属性在首次渲染前就位、无闪烁；模块内还补了系统明暗变化的广播，因为上游 `ActionBar.isDark` 的监听带 `mode === "system"` 守卫、fork 主题下不会响应 | 我方（`services/forkScheme.ts`、`components/ForkSchemeSwitch.tsx` 为 fork 独有文件；上游 `FileTree.tsx` 仅 2 行（import + 组件调用）、`appearance.ts` 的 `data-scheme` 读取分支、两份 i18n 各 6 行文案） |
| 标签栏滑动指示器（`ForkGlassTabs` + `forkTabIndicator`） | 上游标签是「选中项直接上底色」，切换没有任何过渡。fork 换成滑动指示器 + 弹簧形变（参数复刻 bilibili liuli-ui：位置 k=300 ζ=0.5 欠阻尼会回弹，拉伸/挤压与速度联动才有「液体感」；纯 CSS 的 cubic-bezier 做不出联动，只能 rAF 跑真弹簧）。<br>两条落地路径，因为两处标签栏的宿主完全不同：侧栏由 fork 组件 `ForkGlassTabs` 直接渲染；首页看板那套长在 `App.tsx` 中段的上万行 JSX 里，换组件等于往后每次合并都在巨型文件里解冲突，于是改由 `services/forkTabIndicator.ts` 在 DOM 层注入指示器。后者只依赖 `role="tablist"` / `role="tab"` / `aria-selected` 三个 ARIA 属性，比 class 名和 inline style 结实；弹簧参数抽到 `services/forkSpring.ts` 供两边共用，否则同一个应用里两种标签栏手感会不一致。<br>易踩点：配色变量必须挂在 `[role="tablist"]` 上而不是 `[data-fork-glass-tabs]`——只挂后者时，被增强的上游标签栏拿不到 `--fork-tabs-*`，会退回 fallback 的实心强调色，跟侧栏对不上。指示器用 `absolute` 定位在可横向滚动的容器内，测量位置要把 `scrollLeft` 加回去 | 我方（`components/ForkGlassTabs.tsx`、`services/forkSpring.ts`、`services/forkTabIndicator.ts`、`fork-theme.css` 的标签栏段为 fork 独有；上游 `components/FileTree.tsx` 2 行（import + 组件调用）、`ForkShell.tsx` 的 `useTabIndicators` 一处调用。上游改标签栏 DOM 结构时指示器**静默失效**，退化成上游原样，不影响功能） |
| Onboarding 引导锚点与 fork 组件（v0.4.7） | 上游 v0.4.7 新增 onboarding 引导（`OnboardingTour.tsx`），用 `querySelector("[data-onboarding='xxx']")` 定位锚点。fork 的 `ForkGlassTabs` 替换了上游「项目标签栏」的 DOM（上游是 `<div role="tablist" data-onboarding="project-tabs">`），`ForkShell` 替换了上游 `AppShell` 的 shell 元素，导致 `project-tabs`、`shell` 两个锚点在 fork 下**静默失效**——引导走到对应步骤时 `querySelector` 返回 null，聚光灯卡片退化成全屏居中、锚不到目标元素，不报错、`typecheck` 也抓不到。<br>处理：`ForkGlassTabs` 增加 `dataOnboarding` 透传属性（`FileTree.tsx` 传 `project-tabs`），`ForkShell` 的 shell 元素加 `data-onboarding="shell"`。上游日后新增/改名 `data-onboarding` 锚点时，需同步检查 fork 组件（`ForkGlassTabs` / `ForkShell` / 任何 fork 替换过的上游 DOM）是否覆盖——`OnboardingTour` 里其余锚点（`sidebar-menu` / `project-home` / `main-menu` / `session-actions` / `message-input` 等）都长在未替换的上游文件里，自动继承、无需处理 | 我方（`components/ForkGlassTabs.tsx` 的 `dataOnboarding` 属性、`layout/ForkShell.tsx` 的 `data-onboarding="shell"` 为 fork 独有适配；上游 `FileTree.tsx` 的 `dataOnboarding="project-tabs"` 一行传参、`layout/AppShell.tsx` 的 `data-onboarding="shell"` 一行随上游） |
| Git diff 深色可读性（`fork-theme.css`） | 上游 `GitDiffViewer` 与 `MarkdownViewer` 把 diff 行前景写死成浅色主题用的 `#166534` / `#991b1b` / `#1d4ed8`，深色主题下这三个色落在各自的半透明底色上对比度只有 1.5:1 上下（AA 要 4.5），就是「灰蒙蒙、字看不清」的来源。fork 在 glass 深色与 codex 下把前景换成亮色、底色各补一档；两个组件用的是同一组值，git 面板的 diff 与对话里贴的 ` ```diff ` 代码块一并修好。浅色不覆盖——上游这套值本来就是给浅色配的。<br>**属性选择器必须写 DOM 里的规范化形式，不能照抄源码字面量**：React 是逐属性赋值的（`el.style.color = "#166534"`），浏览器序列化 `style` 属性时把 hex 转成 `rgb(22, 101, 52)`，alpha 末位 0 也会去掉（`rgba(..., 0.10)` → `0.1`）。照抄源码写 `[style*="#166534"]` 一条都匹配不上，且**不报错、只是静默不生效**——用 `innerHTML` 注入的样例验证会得到假阳性（那条路径保留属性原文本），必须按 React 的赋值方式造探针。`var()` 原样保留，`0.14` / `0.22` 这类不受影响。<br>`#1d4ed8` 还兼作强调蓝（`ActionBar` / `SessionList` / `ToolCallCard`），只按颜色命中会误伤，必须与 hunk 专属底色成对匹配 | 我方（`fork-theme.css` 独有，上游文件零改动。上游若把这些色值改走 CSS 变量，本段可整体删除） |
| Android 状态栏跟随应用内主题（`forkStatusBar.ts`） | 应用内手动换主题后 Android 状态栏不沉浸。根因在原生：`MainActivity` 在 decorView 顶部盖了一块 `statusBarScrim` View 当状态栏背景（edge-to-edge 下 `window.setStatusBarColor()` 已失效），而它的颜色只有 `applySystemBarStyle()`（按**系统** uiMode 取 `#020617`/白）和 `MindFSSystemBars` 桥两个来源，页面侧只在 `onPageFinished` 后被拉取过一次。于是系统浅色 + 应用内 codex/glass 深色 = 顶上压一条白边。<br>上游 `main.tsx` 那套 `@capacitor/status-bar` 同步补不上：① `StatusBar.setBackgroundColor` 走的还是 `window.setStatusBarColor()`，被 scrim 盖住；② 它的 `MutationObserver` 只盯 `data-theme`，fork 把明暗拆成正交的 `data-scheme` 后，glass 主题下切明暗时 `data-theme` 恒为 `"glass"`，那个 observer 不响应。<br>fork 新增自执行模块直接推 `MindFSSystemBars` 桥（一次设完 scrim 背景 + window 色 + 图标明暗，是当前唯一真正生效的路径），颜色取每套主题都定义了的 `--mindfs-system-bar-bg`，**不做 `elementFromPoint` 采样**——上游那套采样在 glass 下会采到玻璃层 `rgba(255,255,255,0.075)`，alpha 被丢掉后当成纯白，图标判成深色变黑底黑字。推送用双 rAF + 两次延时补推，让 fork 的值排在上游那条异步 Capacitor 调用之后收尾。<br>原生侧配套改动：`MainActivity` 记住页面推来的 `pageStatusBarColor`，`applySystemBarStyle()` 优先用它，否则 `onResume` / `onConfigurationChanged` 会把 scrim 打回系统深浅色。<br>Harmony 端（`EntryAbility.ets` 硬编码 `statusBarContentColor: '#111827'`）有同类问题，**尚未处理** | 我方（`web/src/services/forkStatusBar.ts` 为 fork 独有文件；上游 `main.tsx` 一行 import、`MainActivity.java` 三处（字段 `pageStatusBarColor` + `applySystemBarStyle` 的兜底分支 + `SystemBarsBridge` 里记值）。上游若把 scrim 颜色改为主动跟随页面，本段可评估删除） |
| ACP 终端能力（`server/internal/agent/acp`） | 上游在 `process.go` 里给 `mindfsClient` 的 5 个 terminal 回调（`CreateTerminal` / `TerminalOutput` / `WaitForTerminalExit` / `KillTerminal` / `ReleaseTerminal`）留了**空实现**（全部返回空响应），但 `Initialize` 又声明 `ClientCapabilities.Terminal: true`——ACP agent（grok 等）信了声明去调用，`CreateTerminal` 拿不到 `terminalId`，后续 `terminal/output` 等请求被 SDK 参数校验拦下，报 `terminalId is required`。fork 补上真实实现：新增 `acp/terminal.go` 的 `terminalManager`（terminalId→`runningTerminal` 映射，基于 `commandexec.Start` 执行命令并缓冲输出供 poll，`OutputByteLimit` 按 ACP 规则从开头截断、按 UTF-8 字符边界对齐），5 个回调改为读写该管理器；`Start` / `OpenOptions` 增加 `Shells []commandexec.ShellSpec` 参数，从 `pool.go` 的 `cfg.Shells` 转换传入（保留 Windows UTF-8 编码前缀），`CloseSession` / `Close` 时清理对应 terminals。模型与官方一致：**非交互、一次命令执行 + 输出捕获**（ACP client 接口本就没有写 stdin 的方法）。<br>易踩点：命令必须用 `context.Background()` 启动，不能用 ACP 请求的 ctx——请求返回后 ctx 会被取消，Unix 走 `CommandContext` 直接杀进程；`TerminalOutput` 与 `WaitForTerminalExit` 需要区分「terminal 不存在」和「请求 ctx 取消」两种情况 | 我方（`acp/terminal.go`、`acp/terminal_test.go` 为 fork 独有文件；上游 `acp/process.go` 的 5 个空方法 + `Start` 签名、`acp/session.go` 的 `OpenOptions`、`acp/importer.go` 一行 `Start` 调用、`agent/pool.go` 的 `poolShellSpecs` + `OpenOptions` 传参。上游若日后自己实现 terminal，合并时优先保留上游版本并评估删除本段） |
| ACP 交互式提问 / elicitation（`server/internal/agent/acp`） | 上游 `Initialize` 不声明 elicitation 能力，也没实现 `elicitation/create`，且 Grok 的私有扩展方法 `_x.ai/ask_user_question` 会落进 `HandleExtensionMethod` 的 default 分支返回 `Method not found`——于是 ACP agent 根本无法向用户提问。fork 新增 `acp/elicitation.go`：声明 `ClientCapabilities.Elicitation.Form`，把 `elicitation/create` 与 `_x.ai/ask_user_question` 都桥接到同一条 `askUser` 管线，复用前端既有的 `AskUserQuestionCard`（合成一条 `kind: "ask_user"` 的 tool call，问题塞进 `Meta`，用户的回答经 `session.answer_question` 回流）。requestedSchema 的 `enum` / `items.enum` 映射成单选/多选，回答按 schema 声明的类型（boolean/number/integer）还原后作为 `content` 返回。<br>**易踩点（都是踩过的）**：<br>① **投递必须按 session 串行**：SDK 的通知走单条 `processNotifications` goroutine，但每个带 id 的入站请求（elicitation、permission）各起一条 goroutine（`connection.go:412`），而上层 `usecase/session.go` 的 handler 闭包持有无锁的 per-turn 状态（`auxBuffer` / `thoughtBuffer` / `responseText`）。因此 `sessionState.emit` 加 `emitMu` 串行化，**所有**投递点（含上游原有的 `SessionUpdate` / `RequestPermission`）一律走它，不要再直接调 `getOnUpdate()` 拿到的 handler。<br>② **取消与关会话必须显式释放挂起的提问**：`session/cancel` 不会取消 SDK 的入站请求 ctx（只有 `$/cancel_request` 或断连才会），所以用户点停止后 `askUser` 会一直阻塞到 10 分钟超时，agent 的 turn 收不了尾，且捕获的旧 handler 之后仍会被调用、把状态写进已经 flush 的 `auxBuffer`。`CancelCurrentTurn` / `CloseSession` / `Close` 走 `elicitationRegistry.abandonSession`。<br>③ **完成事件必须带 `Kind`**：`tool_call_update` 自身不携带 kind，而 `appcontext.go` 的看板钩子要靠 `Kind == ToolKindAskUser` 才清 `AskUserWaiting`；漏了它聊天卡片会正常收起、但看板任务永远显示「等待用户」。<br>④ **合成 id 要跨进程重启唯一**：id 会作为 `CallID` 持久化进会话历史，agent 重启后计数器归零，裸计数会与已回答的旧条目撞号，前端把新问题并进旧条目并渲染成「已提交」，用户点不动、只能等超时。故 id 形如 `elic_<随机前缀>_<序号>`。<br>⑤ **超时/取消要返回协议的 decline/cancel 变体而非 JSON-RPC error**：agent 把 `elicitation/create` 上的 internal error 当成客户端故障、直接中止整个 turn。xAI 侧对应的是 `AskUserQuestionExtResponse::SkipInterview`（wire tag `skip_interview`，空结构体变体，只有 `outcome` 一个字段——从 grok 二进制的 serde 字符串确认）。<br>⑥ **答案与超时存在竞态**：`answerElicitation` 先 resolve 再往 buffered channel 发送并返回 nil，此时 WS 侧与 `MarkPendingAskUserAnswered` 都已记「已回答」；若同一瞬间超时/取消分支被 select 选中，答案会被丢弃而 agent 收到 error。故这两个分支返回前用 `drainResolvedElicitation` 非阻塞地再取一次。<br>⑦ 日志只记数量不记内容——用户的自由文本回答可能含凭据。<br>⑧ **agent 的 `_meta` 必须净化、MindFS 自有 meta 走 `TrustedMeta`**：`convertEvent` 不再原样透传 `Raw` 里的 `Meta`——那是 agent 可控的，而 `questions`/`answers`/`title`/`source`/`phase`/`shell`/`filePath`/`path` 等键在 MindFS 里有语义（伪造 `answers` 让卡片变已提交、伪造 `source="userShell"`+`phase="stream"` 劫持 `stream_hub.go` 的 shell 合并路径）。新增 `meta_sanitize.go` 的 `mergeToolCallMeta`：按 `reservedMetaKeys` 黑名单逐键丢弃（记日志不含值），agent meta 序列化超 8 KiB 整体丢弃，最后把 `SessionUpdate.TrustedMeta` 覆盖上去（该字段只在内部赋值、无 wire 表示，伪造不可能）。fork 自己合成的 questions/title/answers 一律塞 `TrustedMeta`，`Kind` 仍留 Raw（看板清标志靠它）。<br>⑨ **schema 枚举还有 `oneOf`/`items.anyOf` 编码**：`enum`/`items.enum` 之外，ACP 允许 `[{const, title}]` 对。`elicitationField` 增加 `enumValues`（label→value 平行切片），解析优先级 `enum` > `oneOf`、`items.enum` > `items.anyOf`，回传时把用户选的 label 换回 const（`labelToValue`），查不到原样透传。<br>⑩ **多选答案的逗号拆分要按已知选项还原**：前端把多选答案用 `", "` 拼成一个字符串，选项本身含逗号（如 `["red, green", "blue"]`）时朴素 `Split(value, ",")` 会拆出不在枚举里的碎片。`splitMultiValueWithOptions` 先整串精确匹配单个 label，再最长匹配重组，失败才回退朴素拆分。<br>⑪ **xAI 响应 map 用问题文本做 key，同文本问题要合并**：`AskUserQuestionExtResponse::Accepted` 的 `answers` 必须是 keyed-by-question-text 的 map（serde 要求，不能改数组），Grok 发两个同文本问题时 `buildQuestionAnswers` 不能覆盖，要合并去重并记日志。<br>⑫ **空 schema 的纯确认请求要能应答**：`UnstableElicitationSchema.Properties` 默认 `{}`，只有 message 的确认请求合法，`len(questions)==0` 且 message 非空时合成 Yes/No 单选确认问题，Yes→Accept（Content 为空 map，不编造键）、No→Decline。<br>**路由的已知局限**：Go SDK 尚未把 elicitation 的 `sessionId` 透给 handler，只能按「最近活跃 session」路由（`SendMessage` / `NewSession` / `ResumeSession` 时认领，`SessionUpdate` 里只有该 session 仍被跟踪才认领，关会话时清掉；找不到且恰好只剩一个活会话时回退到它）。同一 agent 的多个 session 并发提问仍可能投错窗口，SDK 补上 scope 后应改为按 id 路由 | 我方（`acp/elicitation.go`、`acp/elicitation_test.go`、`acp/elicitation_lifecycle_test.go`、`acp/elicitation_bde_test.go`、`acp/elicitation_c_test.go`、`acp/meta_sanitize.go` 为 fork 独有文件；上游 `acp/process.go` 的 `Initialize` 能力声明 + `HandleExtensionMethod` 分支 + `sessionState.emit`/`emitMu` + `activeSessionState` 一组方法 + `CancelCurrentTurn`/`CloseSession`/`Close` 的释放调用 + `wrapSessionUpdate` 签名加 `agentName`、`acp/session.go` 的 `AnswerQuestion`、`convertEvent` 的 `Meta` 走 `mergeToolCallMeta` + `SessionUpdate` 的 `AgentName`/`TrustedMeta` 字段。上游若自行实现 elicitation，合并时优先保留上游版本，但①②③④⑥⑧⑪⑫这几条踩坑要逐条确认上游是否覆盖） |
| 灵动岛按 agent 区分图标（回复轮询接口透出 agent） | fork 自研：小米超级岛「回复中」状态图标按当前驱动 agent 区分。最初试过 animated WebP 星芒呼吸动画，实机确认 SystemUI 对 `miui.focus.pics` 里的动画 drawable 只取静态首帧（RemoteViews 与模板 picInfo 通道均不播放），故**放弃动态图标**，改为「按 agent 名显示对应静态图标」：18 个 agent 图标（claude/codex/augment/cline/copilot/cursor/gemini/grok/hermes/kimi/kiro/omp/openclaw/opencode/pi/qoder/qwen/reasonix）从 `web/public/assets/agents/*.svg` 用 playwright 渲染成透明 PNG（192×192，`res/drawable-xxhdpi/agent_*.png`），`FocusIslandSupport.agentDrawableId` 按 agent 名映射（claude 兼容 `claudecode`/`claude-code`），`agentIcon` 兜底应用图标。为拿到「当前回复由哪个 agent 驱动」，`stream_hub.go` 的 `ReplyingSessionState` 增加 `Agent` 字段、`ListReplyingSessions` 用 `agentOf` 取（优先最新用户消息的 agent，回退队列首条），`http.go` 的 `handleReplyingSessions` 透出 `agent` 键。原生侧 `ReplyPollerService` 解析该字段传给 `FocusIslandSupport.attach`。改动均为纯增量（新增字段 + 新增参数 + 新增图标资源），前端多项目面板只读 `rootId` / `sessionKey`、不受影响 | 我方（`stream_hub.go` 的 `ReplyingSessionState.Agent` + `agentOf`、`http.go` 的 `handleReplyingSessions` 一处键值、`ReplyPollerService.java` 的 `agent` 字段 + 两处 attach 调用、`FocusIslandSupport.java` 的 `agentIcon`/`agentDrawableId`/`hasAgentIcon`/`agentPicRef` + `PIC_AGENT`、`res/drawable-xxhdpi/agent_*.png` 18 个图标。上游若实现类似功能，评估保留上游版本并删本段） |
| Android「需要你输入」通知（ask_user 等待回答透出 `askUserWaiting`） | fork 自研：agent 通过 auq / elicitation 提问并等待用户回答时，Android 端应提示「需要你输入」而不是笼统的「回复中」。后端 `stream_hub.go` 的 `ReplyingSessionState` 增加 `AskUserWaiting` 字段、`ListReplyingSessions` 用 `askUserWaiting(state)` 判定（倒序扫描 `ReplyingList`，找最后一次出现的 `kind=ask_user` 事件，`running`/`pending`/`in_progress`/空状态算等待，`complete`/`failed` 算已离开），`http.go` 的 `handleReplyingSessions` 透出 `askUserWaiting` 键。原生侧 `ReplyPollerService` 解析该字段：等待输入时通知标题「· 需要你输入」、走 `ALERT_CHANNEL_ID` 响铃、`setOngoing` 驻留；`FocusIslandSupport.attach` 增加 `askUserWaiting` 重载，岛状态文案「需要你输入」、`enableFloat`/`islandFirstFloat`/`showHighlightColor` 置真（浮出提醒）。<br>**扩展：问题与选项透出到超级岛展开态**——`ReplyingSessionState` 增加 `AskUserQuestion` 字段（`askUserQuestionText` 从最后一个等待输入的 ask_user 事件的 `Meta["questions"]` 提取「问题\n• 选项1\n• 选项2」多行文本，兼容 JSON 化兜底），`http.go` 透出 `askUserQuestion` 键；`ReplyPollerService` 存进 `ReplyState.askUserQuestion` 并入 `buildStateSignature`（问题变化触发通知更新），`FocusIslandSupport.attach`/`buildCard` 加 `askUserQuestion` 参数，展开态摘要区优先显示问题+选项而非笼统占位。改动均为纯增量。已知边界：判定依赖事件流里 ask_user 的 status 与 Meta["questions"] 的结构，若上游改了合成事件的 status 取值或 questions 承载方式需同步 | 我方（`stream_hub.go` 的 `ReplyingSessionState.AskUserWaiting`/`AskUserQuestion` + `askUserWaiting`/`askUserQuestionText`/`askUserQuestionTextFromAny`/`stringValueOf`、`http.go` 的 `handleReplyingSessions` 两处键值、`ReplyPollerService.java` 的 `askUserWaiting`/`askUserQuestion` 字段 + `replyStatusText`/`hasAskUserWaitingState`/`buildReplyNotification`/`buildReplyChildNotification`/`buildReplyGroupSummaryNotification`/`buildStateSignature`、`FocusIslandSupport.java` 的 `askUserWaiting`/`askUserQuestion` 重载 + `chipText`/`summaryPlaceholder`/`buildCard`/`buildParams`、新增 `stream_hub_askuser_test.go`（含问题格式化用例）。上游若实现类似功能，评估保留上游版本并删本段） |
| `docs/windows-redeploy.md` + `CLAUDE.md` 的「部署到本机（Windows）」小节 | fork 独有：本机部署规范。会话本身跑在 `mindfs.exe` 进程树里，而停服务走 `taskkill /T`（树杀），在会话里直接停 mindfs 会把 Agent 自己一起杀死、部署卡在半路——因此部署必须交给脱离进程树的计划任务。文档记录四任务权限委派体系（进程继承启动者令牌 + 任务 RunLevel 创建时固定，故一个任务盖不住两种权限，需委派）、以及一批只在真机踩到的坑：`/RL HIGHEST` 不可省（普通权限杀不动管理员进程）、`taskkill` 的 stderr 在 `ErrorActionPreference=Stop` 下抛 `NativeCommandError` 且 `2>&1 \| Out-Null` 拦不住、`Get-CimInstance` 在计划任务令牌下返回空导致漏杀、WMI 起 cmd 需 `ShowWindow=0` 否则弹窗常驻、脚本 UTF-8 无 BOM 而 PowerShell 5.1 按 GBK 读因而注释须用英文。脚本本体（`redeploy.ps1` / `setup-tasks.ps1`）在 `%LOCALAPPDATA%\Programs\mindfs\`，不在仓库内 | 我方 |
| `release-notes.md` | 跟随上游发版记录，fork 不自行发版 | 上游 || `go.mod` / `go.sum` 的 `replace` 指令 | 指向 `github.com/yandc/*`，由上游维护 | 上游 |

### 暂未分歧、但日后改动时须登记的敏感点

以下是上游与 a9gent.com 商业服务的挂钩点。目前 fork **尚未改动**它们；一旦改动（如指向自建服务），必须在上表登记：

- `agents.json` 的 `relayBaseURL` / `tokenStationURL`（官方中继与 Token 中转站入口；运行时可用 `MINDFS_RELAY_BASE_URL` 环境变量覆盖，优先走这条路而不是改代码）
- `server/internal/update/service.go` 与 `server/app/server.go` 中硬编码的 `a9gent/mindfs` 仓库名、`relay.a9gent.com/mindfs-downloads` 下载源（自更新通道）
- `server/internal/relay/tips.go`（官方推广卡片轮询，`-norelayer` 或清空 relayBaseURL 即不请求）
- `scripts/install.sh` / `scripts/install.ps1` 中的仓库名与下载地址

## 上游同步流程

跟着上游的 **release tag** 合并（而不是追每个 commit）。步骤：

1. 工作区必须干净（`git status` 无未提交改动）。
2. `git fetch upstream --tags`
3. `git merge <tag>`（如 `git merge v0.4.4`；merge 而非 rebase，保留独立 merge commit，不 squash）
4. 解冲突：按上表「冲突归属」处理；表里没有的冲突按常规判断并考虑是否登记。
5. 门槛检查：`go test ./...` 和 `cd web && npm run typecheck` 全部通过。
   注意：Windows 本地跑 `go test ./...` 存在已知的环境性失败（POSIX 文件权限断言、SQLite 临时目录文件锁、部分测试会读到真实用户目录的 skills / 项目注册表），**以 CI 的 Linux 结果为权威门槛**，本地失败时先对照 CI 判断是否为环境问题。
6. 回归扫描：`git grep a9gent.com -- ':!*.md'` 对比合并前后，确认上游新增代码没有引入绕过配置的硬编码官方地址；有则评估处理并登记。
7. 布局层扫描：`git log <上次合并的 tag>..<本次 tag> --oneline -- web/src/layout/AppShell.tsx`。fork 的 `ForkShell` 接管了布局，**不会自动继承上游对 `AppShell` 的改动**，因此有输出就要逐条评估是否 port。
   - 优先看移动端修复（`fix.*mobile` / `ios` / safe-area / IME / viewport height 这类）：`ForkShell` 里的 `--mindfs-safe-area-top`、`--mindfs-viewport-height`、`willChange`、`backfaceVisibility`、`transform: translateZ(0)` 全是上游踩坑换来的补丁，漏掉在桌面浏览器上测不出来，只会在真机上炸。
   - 主题/配色类改动通常已被 `--fork-*` 变量吸收，确认一遍即可。
   - 需要对照上游布局效果时，页面 URL 加 `?forkShell=0` 可临时切回上游 `AppShell` 渲染。
   - props 形状变化不用靠人眼：`ForkShell` 内的契约断言会让 `npm run typecheck` 直接失败。
8. 全部通过后 push，等 CI 变绿。

## 提交规范

- fork 自己的改动：提交信息加 `fork:` 前缀（如 `fork: 支持自建中继地址`），方便 `git log` 区分来源。
- 上游合并：保留默认 merge commit 信息（`Merge tag 'v0.4.4' ...`）。
